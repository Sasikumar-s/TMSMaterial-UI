import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sasikumar.tms',
  appName: 'tmsmaterial',
  webDir: 'dist/tmsmaterial',
  bundledWebRuntime: false
};

export default config;
